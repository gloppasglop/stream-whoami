apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: stream-dev-whoami

resources:
  - ../base/

patches:
  - path: namespace.yaml
  - path: deployment-whoami.yaml
  - path: ingress-whoami.yaml
